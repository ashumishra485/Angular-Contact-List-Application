language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ng-module' ]; then cd .. && eval "mv $currentfolder generator-ng-module" && cd generator-ng-module; fi

